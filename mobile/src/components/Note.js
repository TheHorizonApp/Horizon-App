import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Note = () => {
  return (
    <View>
      <Text>Note</Text>
    </View>
  )
}

export default Note

const styles = StyleSheet.create({})